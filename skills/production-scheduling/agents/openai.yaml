interface:
  display_name: "Production Scheduling"
  short_description: "Preview today's plan and safely apply scheduling changes"
  default_prompt: "Use $production-scheduling to preview pending orders for today, attach the CLI-generated HTML Gantt chart, and apply only after I confirm."
