interface:
  display_name: "LAPS CLI Authentication"
  short_description: "Sign in, inspect, or revoke personal LAPS CLI OAuth access"
  default_prompt: "Use $laps-cli-auth to authenticate this LAPS CLI operation with my personal permissions."
